version: '3.8'

services:
  motel-bot:
    build: .
    container_name: motel-intensy-bot
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./sessions:/app/tokens
      - ./temp:/app/temp
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 3072M
        reservations:
          memory: 1024M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001/login"]
      interval: 1m
      timeout: 10s
      retries: 3
    restart: always
