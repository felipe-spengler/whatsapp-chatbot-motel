version: '3.8'

services:
  motel-bot:
    build: .
    container_name: motel-intensy-bot
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./sessions:/app/tokens
      - ./temp:/app/temp
    restart: always
